# Listen on all interfaces so LAN devices can connect (default).
HOST=0.0.0.0
PORT=3001

# Comma-separated browser origins allowed in production.
# In dev, localhost and private LAN IPs (192.168.x.x, 10.x.x.x, etc.) are also allowed.
CLIENT_ORIGIN=http://localhost:5173,http://192.168.1.189:5173
